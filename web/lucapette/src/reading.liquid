---
layout: base.liquid
title: Reading
permalink: /reading/
description: Notes on books I've read
---

<h1>Reading</h1>

{% if collections.reading.size > 0 %}
    <div class="books-grid">
        {% for item in collections.reading %}
            <a href="{{ item.url }}">
                <article class="book-card">
                    <div class="book-card-cover">
                        <img
                            src="/assets/reading/{{ item.fileSlug }}.jpg"
                            alt="Cover of {{ item.data.title }}"
                            loading="lazy"
                        />
                    </div>
                    <footer class="book-card-info">
                        <h3>{{ item.data.title }}</h3>
                        {% if item.data.author %}
                            <p class="book-card-author">{{ item.data.author }}</p>
                        {% endif %}
                    </footer>
                </article>
            </a>
        {% endfor %}
    </div>
{% else %}
    <p>No reading notes yet. Check back soon!</p>
{% endif %}
